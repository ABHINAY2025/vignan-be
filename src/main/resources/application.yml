server:
  port: ${PORT:8082}

firebase:
  service-account: classpath:firebase-service-account.json

spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

logging:
  level:
    com.vignanlive: DEBUG
    com.google.cloud.firestore: WARN
